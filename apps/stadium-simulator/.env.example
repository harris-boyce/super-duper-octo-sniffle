# Optional: Override API endpoint (defaults to /api/announcer)
# VITE_API_ENDPOINT=http://localhost:3000/api/announcer
VITE_DEBUG_PATHFIND=false

# Deployment target (itch, vercel, github)
# Controls base path for asset loading
# VITE_DEPLOY_TARGET=itch